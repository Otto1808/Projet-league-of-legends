export default function page(){
    for (let i = 0; i < 4; i++) {

        let x: number = Math.floor(Math.random() * 101);
        let y: number = Math.floor(Math.random() * 101);

        console.log("-",x,",", y)
        return(
            <main>

            </main>
        )
    }
}